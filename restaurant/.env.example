# Restaurant example — environment variables
# Copy to .env and fill in your values.

# LLM provider (pick one)
GEMINI_API_KEY=
# GROQ_API_KEY=
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=

# MCP gateway token (any value — dev_bypass=true ignores it)
MCP_TOKEN=demo-token
