# External CLI delegation example — environment variables
# Copy to .env and fill in your values.

# LLM provider API key (required unless using Ollama locally)
GEMINI_API_KEY=
# OPENAI_API_KEY=

# MCP gateway token (any value — dev_bypass=true ignores it)
MCP_TOKEN=demo-token
