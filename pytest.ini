[pytest]
asyncio_mode = auto
filterwarnings =
    ignore::pytest.PytestUnknownMarkWarning
markers =
    example(name): name of the example under test
    live: live-server test requiring docker infrastructure
